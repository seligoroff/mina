[pytest]
addopts = --cov=app --cov-report=term-missing
testpaths =
    tests/unit
    tests/integration
python_files = test_*.py
markers =
    unit: marks tests as unit tests
    integration: marks tests covering multiple layers/end-to-end flows

